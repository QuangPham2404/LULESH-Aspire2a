# 2026-08-05 Progress

## Session Summary

Completed a more intensive environment compilation sweep and refined the
workflow documentation. The Cray, AOCC, Intel, and GNU programming
environment cases now have build/run records using the existing LULESH result
schema.

## Workflow and Documentation

- Added the Step 1 project-overview procedure to `AGENTS.md`.
- Clarified that `APPLICATION.md` is the application overview and that
  optimization planning belongs in `planning/`.
- Added the root `APPLICATION.md` for LULESH, including the source revision,
  build/run overview, baseline command, flags, and correctness markers.
- Added probe-script rules covering read-only scope, raw output retention,
  automatic probe submission, and the boundary between probing and analysis.
- Revised Step 5 so an already-decided schema allows compatible runs to
  proceed automatically. Step 5 validates expected experiment output and does
  not recheck PBS state or exit status.
- Clarified preflight checks: only tools directly required by build, run, or
  validation may be fatal; optional provenance tools must not block execution.

## Hardware Probe

- PBS job: `15089995.pbs101`
- Node: `x1001c3s2b1n0`
- Hardware: 2-socket AMD EPYC 7713, 128 physical cores, 256 logical CPUs,
  8 NUMA nodes, approximately 503 GiB memory, and HSN networking.
- No NVIDIA or ROCm accelerator tools were detected.
- Raw probe output is tracked under `scripts/outputs/`.

## AOCC Workflow Test

- Built with `PrgEnv-aocc/8.3.3`, AOCC 3.2.0, and Cray MPICH 8.1.15.
- Successful build job: `15090035.pbs101`.
- Successful smoke run: `15090036.pbs101`, `-s 10`.
- LULESH elapsed time: `0.26 s`.
- FOM: `895.58097 z/s`.
- Correctness passed.
- Two initial failures were recorded and corrected:
  - `15090029.pbs101`: submitted from the wrong directory, so relative PBS
    output paths resolved to a missing root-level `outputs/` directory.
  - `15090033.pbs101`: the build preflight incorrectly required `git` on the
    compute node. Git was removed from required compute-node tools and the
    source revision was passed as metadata.

## Intel and GNU Environment Sweep

- Intel build job: `15090185.pbs101`; run job: `15090189.pbs101`.
- GNU build job: `15090186.pbs101`; run job: `15090190.pbs101`.
- Both builds and runs completed with PBS exit status `0` and produced normal
  application output.
- Intel correctness failed because `MaxRelDiff = -nan`; it is recorded as
  `run_status=success`, `correctness_status=failed`.
- GNU correctness passed with `MaxRelDiff = 3.938811e-14`.
- GNU LULESH elapsed time: `0.34 s`; FOM: `682.15817 z/s`.
- Intel LULESH elapsed time: `0.20 s`; FOM: `1182.5021 z/s`, retained as a
  completed but correctness-failed record.

## Results

- `results/metrics.csv` now contains four runs: Cray, AOCC, Intel, and GNU.
- `results/RESULTS.md` was regenerated from the CSV.
- The extraction script is append-safe, rejects duplicate attempts, preserves
  the existing schema, and classifies non-finite correctness values as failed.
- All sweep raw PBS logs and result records were pushed to GitHub.

## Commits Pushed

- `bf979e1`: Add compute node hardware probe.
- `d1515e6`: Record compute node hardware probe.
- `0b11b51` through `808689b`: Add and correct AOCC workflow attempts.
- `4d0826d`: Record AOCC build and smoke run.
- `ba2cb05`, `1ac406b`: Make Step 5 extraction append-safe and executable.
- `2feeaa3`: Add Intel and GNU sweep workflows.
- `39b6c17`: Record Intel and GNU environment sweep.

## Handoff

The remaining local changes are the updated `AGENTS.md`, the existing root
`README.md` change, the new `APPLICATION.md`, and this progress note. Commit
and push them from the local clone, then synchronize Aspire2A with
`git pull --ff-only`. No optimization conclusions were added to `planning/`.
