# 2026-08-05 Progress — Session 2

## Session Summary

Defined and committed the explicit error-handling workflow, then established
the larger `-s 30` compiler-environment baseline using the existing successful
Release binaries. All four jobs requested one MPI rank, one OpenMP thread, and
`16gb` of memory. The runs completed and their raw records and extracted
results were pushed to GitHub and synchronized with Aspire2A.

## Error-Handling Workflow

- Added a dedicated `Error-patching procedures` section to `AGENTS.md`.
- Defined Track 1 for deterministic, low-risk workflow defects that may be
  automatically patched and retried.
- Defined Track 2 for errors requiring manual inspection, user judgment,
  external authorization, or changes beyond routine workflow repair.
- Defined the scientific-correctness exception: continue result logging,
  do not self-patch by default, and add a note to `results/RESULTS.md`.
- Defined the structured `OVERRID_AUTO_PATCH` authorization format with
  explicit error class, allowed action, scope, and restrictions.
- Defined `MANUAL_INSPECTION_ERROR.md` as the future append-only case log for
  Track 2 failures.

## Planning and Run Preparation

- Updated `planning/PLANS.md` with the staged optimization sequence:
  stable baseline, OpenMP scaling, MPI/hybrid scaling, compiler flags, and
  separate Intel correctness investigation.
- Confirmed that the `-s 30` baseline reused the existing four successful
  build binaries; no rebuild was required.
- Added four run cases under `experiments/`:
  - `CCE13-CrayMPICH8-baseline-s30`;
  - `AOCC-CrayMPICH8-baseline-s30`;
  - `Intel-CrayMPICH8-baseline-s30`; and
  - `GNU-CrayMPICH8-baseline-s30`.
- Each run case has a PBS script, README, and designated `outputs/` path.
- Each PBS job requested `select=1:ncpus=1:mpiprocs=1:mem=16gb` and
  `walltime=00:10:00`.
- All four PBS scripts passed `bash -n` validation.

## `-s 30` Baseline Runs

| Environment | PBS job | Node | PBS state/exit | LULESH time | FOM | Correctness |
| --- | --- | --- | --- | ---: | ---: | --- |
| Cray CCE 13.0.2 | `15093829.pbs101` | `x1003c2s1b1n1` | `F / 0` | 20 s | 1281.9581 | passed |
| AOCC 3.2.0 | `15093830.pbs101` | `x1003c2s1b1n1` | `F / 0` | 32 s | 778.03348 | passed |
| Intel 2024.0 | `15093831.pbs101` | `x1001c2s7b1n0` | `F / 0` | 25 s | 1019.1226 | failed: `MaxRelDiff = -nan` |
| GNU 11.2.0 | `15093832.pbs101` | `x1001c2s7b1n0` | `F / 0` | 27 s | 941.23381 | passed |

All runs reported problem size `30`, one MPI task, one OpenMP thread, and
932 iterations. The CCE, AOCC, and GNU correctness fields were finite. The
Intel run reproduced the earlier non-finite correctness result and was kept as
`run_status=success`, `correctness_status=failed`; no patch was attempted.

## Results and Synchronization

- Retrieved and retained all eight raw PBS `.o`/`.e` files under the four new
  experiment `outputs/` directories.
- Appended four compatible rows to `results/metrics.csv`.
- Regenerated `results/RESULTS.md`.
- Updated the extraction script so future report regeneration preserves a
  correctness note for non-finite failed results.
- Committed result records as `8d8e383` (`Record s30 compiler baseline results`).
- Pushed the commit to GitHub.
- Aspire2A initially had untracked copies of the same eight output files, so
  the fast-forward pull was blocked. Their SHA-256 hashes were verified
  against the local committed files, only those exact duplicate files were
  removed, and Aspire2A then fast-forwarded to `8d8e383`.
- Pre-existing unrelated untracked artifacts on Aspire2A were left untouched.

## Commits This Session

- `b61d69d`: Document error-patching procedures.
- `ecb7d82`: Add `-s 30` compiler baseline run cases and update plans.
- `8d8e383`: Record `-s 30` compiler baseline results.

## Handoff

- The four `-s 30` baseline results are recorded and synchronized.
- The Intel correctness failure remains unresolved and must not be used as a
  valid optimization result without further investigation.
- The next planned work is to repeat the larger valid baseline for timing
  stability, then run the AOCC OpenMP thread-scaling sweep with the same binary,
  problem size, and controlled resources.
- No new result columns or schema changes were introduced.
