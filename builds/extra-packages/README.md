# `builds/extra-packages/`

Place explicitly approved local dependencies or vendored packages here only when
needed to support builds.

Prefer Aspire2A environment modules. Ask for user approval before adding or
installing software that is not already available.
