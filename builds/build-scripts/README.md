# `builds/build-scripts/`

Place reusable build scripts here.

These scripts are kept for record and handoff. They should capture how a build
was produced, including modules, compilers, build flags, and output locations.
